defaults: &defaults
  access_key_id: ""
  secret_access_key: ""
  host: s3.amazonaws.com

development:
  <<: *defaults
  uploads_bucket_name: "junction-dev"
  static_bucket_name: "junction-dev"

test:
  <<: *defaults
  uploads_bucket_name: "junction-dev"
  static_bucket_name: "junction-dev"

staging:
  <<: *defaults
  uploads_bucket_name: "junction-dev"
  static_bucket_name: "junction-dev"

production:
  <<: *defaults
  uploads_bucket_name: "junction-prod"
  static_bucket_name: "junction-prod"